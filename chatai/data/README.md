This folder holds your local runtime data: characters, personas, chats,
and settings, all as plain JSON files (plus any uploaded avatar images).

It's gitignored on purpose — this is your private chat data and should
never be committed or pushed anywhere. Back it up yourself if you want
to keep it (it's just files, so `cp -r data/ backup/` is enough).
